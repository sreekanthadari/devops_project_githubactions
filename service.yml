apiVersion: v1
kind: Service

metadata:
  name: docker-project-service
  namespace: devops-project

spec:
  selector:
    app: docker-project-1

  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000

  type: NodePort
